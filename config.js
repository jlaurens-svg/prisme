/* Configuration du site.

   Adresse du relais qui détient la clé d'API pour la médiation du Miroir
   (voir worker/README.md pour le déployer). Tant que cette valeur est vide,
   la médiation propose le mode « clé personnelle » : chaque visiteur doit
   fournir sa propre clé, qui reste dans son navigateur.

   Ne mettez JAMAIS de clé d'API dans ce fichier : il est public. */
window.PRISME_AI_ENDPOINT = "";
